# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Server URL for generating figure URLs
# Change this to your production domain if deploying
SERVER_URL=http://localhost:8000

# Optional: Enable debug mode (not recommended for production)
# DEBUG=True
